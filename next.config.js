/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export',
  
  // Mantenha outras configurações que você tenha
  // experimental: {
  //   appDir: true,
  // },
}

module.exports = nextConfig